extends layout

block content
  h1= title
  p Welcome to my web apps assignment 10
  button(type="button" class="btn btn-secondary") Press me 

   div#faster Add in extra

  script.
    let value = 1;
    let amt = 2;

    function faster() {
      amt *= 2;
      value += amt;
      document.getElementById("faster").innerText = `Faster ${value} times ${amt}`;
    }

